function minMax(numbers) {
    return "The maximum is: "+Math.max(...numbers) + " and the minimum is: "+ Math.min(...numbers);
    }

numbers = [10,22,334,1,323,1122,0];
console.log(minMax(numbers));

